# CLAUDE.md — 이 프로젝트 전용 규칙

> ponytail lite는 계속 켜둡니다. 아래는 "코드량을 줄이지 말라"는 뜻이 아니라,
> **실무 코드 리뷰에서 실제로 걸리는 지점들**을 정리한 것입니다.
> 패키지 구조/네이밍처럼 사소한 건 자유롭게 판단하고, 대신 판단 이유를 남기세요.

## 이 프로젝트의 목적
`docs/PROJECT_SPEC.md` 참고. 지금은 Phase 1(계좌이체 & 잔고관리).
목표는 "동작하는 코드"가 아니라 **주니어 백엔드가 실무에서 바로 투입돼도 되는 수준의 코드**를 만들고,
왜 그렇게 짰는지 설명할 수 있는 것.

## 원칙: 구조는 판단, 안전장치는 타협 없음

### 자유롭게 판단해도 되는 것 (정답 없음, ponytail 재량)
- 패키지를 도메인별로 쪼갤지 레이어별로 쪼갤지
- 인터페이스를 둘지 말지 (Service를 인터페이스+구현체로 나누는 건 실무에서도 팀마다 다름 — 지금처럼 구현체가 하나뿐이면 인터페이스 생략해도 됨. "나중에 확장될 수도 있으니 미리 인터페이스"는 실무에서도 지양하는 과설계)
- DTO를 record로 할지 class로 할지
- Lombok 사용 범위 (`@Data`는 지양 — equals/hashCode/toString이 엔티티에서 무한루프나 프록시 문제 일으키는 경우가 실무에서 흔함. `@Getter`, `@Builder`, `@RequiredArgsConstructor` 정도로 제한 권장)

### 타협 없이 지켜야 하는 것 (실무 코드리뷰 기준)

**1. 테스트는 H2가 아니라 Testcontainers**
- H2는 MySQL 문법/락 동작이 실제와 달라서 실무에서는 통합테스트 신뢰도가 떨어진다는 이유로 점점 안 씀.
- `spring-boot-testcontainers` + MySQL 컨테이너로 통합테스트 작성. (단위 테스트는 Mockito로 충분, 굳이 컨테이너 안 띄워도 됨 — 계층 구분할 것)
- 동시성 테스트(IS-12, IS-13)는 특히 실제 MySQL 락 동작을 봐야 의미가 있어서 Testcontainers 필수.

**2. 트랜잭션 경계는 명시하되, 읽기 전용 최적화도 챙길 것**
- 쓰기 트랜잭션: `@Transactional`
- 조회 전용 메서드: `@Transactional(readOnly = true)` — 실무에서 안 하면 리뷰에서 반드시 걸림 (더티체킹 오버헤드 방지)
- 서비스 계층에만 `@Transactional` 부여. Controller/Repository에 붙이지 않음.

**3. N+1 인지**
- 연관관계 조회 시 `fetch join` 또는 `@EntityGraph`로 명시적으로 방지.
- "일단 짜고 나중에 최적화"가 아니라, 연관 엔티티를 join해서 가져오는 지점에서 바로 확인 (테스트에 쿼리 카운트 검증 추가하면 베스트, 최소한 로그로 SQL 확인).

**4. 설정/시크릿 분리**
- DB 계정, 포트 등은 `application.yml`에 하드코딩 금지. `application-local.yml` / 환경변수 / `.env`(gitignore) 사용.
- `application.yml`에는 profile 분기(local/test/prod)만 두고 실제 값은 넣지 않음.

**5. 예외 처리는 구조화**
- `ErrorCode` enum에 HTTP status까지 매핑 (`INVALID_INPUT(400)`, `INSUFFICIENT_BALANCE(409)` 등).
- 비즈니스 예외(잔고부족, 계좌없음 등)와 시스템 예외(DB 오류 등)를 구분해서 잡을 것.
- 예외를 삼키거나 `e.printStackTrace()`로 때우지 않음 — 로거 사용.

**6. 동시성 코드는 왜 그 방식인지 주석 필수**
- 낙관적 락 vs 비관적 락 vs 분산 락 중 왜 이걸 골랐는지 한 줄. (Phase별로 다른 이유가 `PROJECT_SPEC.md`에 있음 — 그대로 가져다 쓰지 말고 본인 말로 재정리)

**7. 로깅은 운영 관점으로**
- 정보성/경고/에러 레벨 구분.
- 민감정보(계좌번호 전체, 잔고 등)는 로그에 그대로 찍지 않음 — 마스킹 습관을 들이는 게 금융권 실무에서 중요.

**8. API 스펙 문서화**
- SpringDoc OpenAPI(Swagger) 붙여서 `/swagger-ui.html`에서 확인 가능하게. README에 curl 예시만 있는 것보다 실무에 훨씬 가까움.

## 진행 방식 (이건 계속 유지)
- IS 단위로 끊어서 진행, 완료 시 요약 보고.
- IS-12(버그 재현) → IS-13(해결) 사이는 반드시 멈춰서 실패를 직접 확인.
- 큰 구조적 판단(인터페이스 도입 여부, 패키지 분리 방식 등)은 ponytail이 알아서 결정하고 "왜 이렇게 했는지" 1~2줄로 설명하면 됨 — 매번 물어볼 필요는 없음.

## 커밋 컨벤션
- `feat(IS-9): 입금/출금 API 구현`
- `fix(IS-13): 낙관적 락 + 재시도로 동시성 버그 해결`
- `test(IS-12): Testcontainers 기반 동시성 재현 테스트`