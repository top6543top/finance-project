apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: account-service
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: account-service
                port:
                  number: 80